# Workspace queue exploration — superseded

The queue/scheduler proposal has been replaced by the simple Markdown worklist.

See [Workspace worklist specification](workspace-worklist.md) and the
[single Good / Snazzy mock](../../visual-review/workspace-queue.html).

The settled direction is quick capture plus an ordinary closable worklist tab.
There are no agent claims, mandatory review stages, or queue database.
